<% if @users.length.zero? %>
<strong>No users were found. Why don't you create one?</strong>
<% else %>
<table>
  <tr>
    <td>ID</td>
    <td>Username</td>
    <td>Email</td>
    <td>Actions</td>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= user.id %></td>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
    <td>
      <%= link_to '🖊', edit_user_path(user) %>
    </td>
  </tr>
  <% end %>
</table>
<% end %>

<p>
  <%= link_to 'New User', new_user_path %>
</p>
